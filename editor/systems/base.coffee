module.exports = class System
	constructor: (@client) ->
		# Do some init
	destroy: ->
		# Do some destroy
	prepare: (entity) ->
		# Initialize this entity
	attach: (entity) ->
		# Do some attach
	detach: (entity) ->
		# Do some detach